import { default as errorButton } from './global/error-button';
import { default as infoButton } from './global/info-button';
import { default as startButton } from './global/start-button';
import { default as startButtonFocus } from './global/start-button-focus';
import { default as warnButton } from './global/warn-button';

export const SVGGlobalIcons = {
	errorButton,
	infoButton,
	warnButton,
	startButtonFocus,
	startButton
};
