import { default as UIIconPending } from './icon-pending/index.svelte';
import { default as UIPrepareData } from './prepare-data/index.svelte';

export namespace BCUI {
	export const IconPending = UIIconPending;
	export const PrepareData = UIPrepareData;
}
